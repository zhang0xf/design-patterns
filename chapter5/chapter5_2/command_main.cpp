#include "command.h"

// 为创建一个调用(MyClass)类的一个实例上的(Action)的(Command)对象, 需要:
// 这一方案只适用于简单命令
class MyClass {
public:
    // ... TODO(Action???)
};

int main() {
    // MyClass *receiver = new MyClass;
    // ...
    // Command *aCommand = new SimpleCommand<MyClass>(receiver, &MyClass::Action);
    // ...
    // aCommand->Execute();

    // TODO
    // Command案例

    return 0;
}